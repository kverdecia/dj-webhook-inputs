============
Installation
============

At the command line::

    $ easy_install dj-webhook-inputs

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-webhook-inputs
    $ pip install dj-webhook-inputs
